function sendBasket(img, title, size, toppings){
    let sendtoBasket = {
        photo: photo,
        name: name,
        size: size,
        toppings: toppings,
    };

    sendBasket.photo = img;
    sendBasket.name = title;
    sendBasket.size = size;
    sendBasket.toppings = toppings;
}